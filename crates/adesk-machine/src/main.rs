//! `adesk-machine` binary — host-side AI Machine lifecycle CLI.
//!
//! Implementation in progress. The planned CLI (create/start/stop/remove/list/
//! status, `--runtime podman|mock`) is documented in `CONTEXT.md`; the design is
//! `docs/machine.md`.

#![forbid(unsafe_code)]

fn main() {
    eprintln!("adesk-machine: not yet implemented (see crates/adesk-machine/CONTEXT.md)");
    std::process::exit(2);
}

//! `adesk-test-app` — helper process that opens a real toplevel and exits on command.
//!
//! PLACEHOLDER — the full CLI (arg grammar documented in the real module) is
//! written during the same architecture phase. This stub keeps the `[[bin]]`
//! target compiling.

fn main() {
    todo!("stub: replaced by the real adesk-test-app helper")
}

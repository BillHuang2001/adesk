//! Headless `adesk-viewer` binary — a VAP client.
//!
//! Implementation in progress. The planned CLI (transport selection, frame
//! capture, scripted input) is documented in `CONTEXT.md`; the protocol is
//! `docs/viewer.md`.

#![forbid(unsafe_code)]

fn main() {
    eprintln!("adesk-viewer: not yet implemented (see crates/adesk-viewer/CONTEXT.md)");
    std::process::exit(2);
}
